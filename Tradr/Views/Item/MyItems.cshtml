@using System.Drawing
@using System.Web.Http
@using Tradr.Models
@model List<Item>
@{
    ViewBag.Title = "My Items";
    ViewBag.Nav = "#nav2";
}

@foreach (var item in Model.OrderByDescending(x => x.DateTimeAdded))
{
        Html.RenderPartial("InfoItemBox", item);
}
